# LobsterPress Dependencies
# Python 3.8+

# Token 计数（可选，无此依赖时使用近似计算）
tiktoken>=0.5.0

# NLI 模型（可选，用于高精度矛盾检测）
# 安装后自动启用 NLI 检测，否则降级为规则检测
# sentence-transformers>=2.2.0

# 开发依赖
# pytest>=7.0.0  # 单元测试
# pytest-cov>=4.0.0  # 测试覆盖率
